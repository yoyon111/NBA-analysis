pip install serpapi openai

pip install google-generativeai

pip install grpcio

pip install google-generativeai serpapi pandas selenium beautifulsoup4 webdriver-manager fake-useragent


In matchup_agent, replace "genai.configure(api_key=os.getenv("GEMINI_API_KEY"))" with gemini key "genai.configure(api_key="KEY")
